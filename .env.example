# Root .env.example
# NOTE:
# - Set these in your Netlify Site > Site settings > Build & deploy > Environment
# - Variables without the REACT_APP_ prefix are NOT injected into the React bundle (good for secrets).
# - Never expose secrets in the frontend build. Keep secrets only in server-side environments (e.g., Netlify Functions).

# -------------------------
# Supabase (Server-side)
# -------------------------
# The base URL of your Supabase project (found in Supabase settings)
SUPABASE_URL=
# The public/anon key for your Supabase project (safe for clients, also usable server-side)
SUPABASE_ANON_KEY=
# The service role key for privileged Supabase operations (SERVER-SIDE ONLY; keep secret)
SUPABASE_SERVICE_ROLE_KEY=

# -------------------------
# LinkedIn OAuth (Server-side)
# -------------------------
# LinkedIn Developer App Client ID
LINKEDIN_CLIENT_ID=
# LinkedIn Developer App Client Secret (keep secret)
LINKEDIN_CLIENT_SECRET=
# The OAuth redirect URI configured in your LinkedIn app settings
# Example (Netlify hosted): https://<your-site>.netlify.app/api/auth/linkedin/callback
# Example (local dev): http://localhost:8888/api/auth/linkedin/callback
LINKEDIN_REDIRECT_URI=

# -------------------------
# OpenAI (Server-side)
# -------------------------
# API key used for AI-powered query parsing and/or embeddings.
# DO NOT expose in frontend builds.
OPENAI_API_KEY=

# -------------------------
# Optional
# -------------------------
# Secret for encrypting tokens or sensitive data at rest in Netlify Functions.
# If provided, functions can perform symmetric encryption of LinkedIn tokens before storage.
NETLIFY_TOKEN_ENCRYPTION_SECRET=

# -------------------------
# React Frontend (Client)
# -------------------------
# These variables are read by Create React App and baked into the client bundle at build time.
# They MUST be prefixed with REACT_APP_ to be accessible in the frontend code.
# Do not add any secrets here.
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_ANON_KEY=
